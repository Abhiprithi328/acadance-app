// Change this ONE line once your Render backend is deployed.
// Everything else in the app imports from here, so you never have to
// hunt through multiple files to update the backend link.
export const API_BASE_URL = 'https://YOUR-RENDER-URL.onrender.com';
